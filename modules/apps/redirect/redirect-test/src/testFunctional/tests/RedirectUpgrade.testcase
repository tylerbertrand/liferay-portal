@component-name = "portal-wcm"
definition {

	property database.types = "db2,mariadb,mysql,oracle,postgresql,sqlserver,sybase";
	property portal.release = "true";
	property portal.upstream = "true";
	property test.assert.warning.exceptions = "true";
	property test.liferay.virtual.instance = "false";
	property test.run.type = "single";
	property testray.component.names = "Upgrades WEM";
	property testray.main.component.name = "URL Redirections";

	setUp {
		SignIn.signIn();

		var portalURL = PropsUtil.get("portal.url");

		AssertLocation.assertPartialLocation(value1 = "${portalURL}/web/guest");

		SearchAdministration.executeReindex();
	}

	@priority = 4
	@refactorneeded
	test ViewRedirectArchive73101 {
		property data.archive.type = "data-archive-redirection";
		property portal.version = "7.3.10.1";

		Redirect.openToConfigInSystemSettings();

		Redirect.viewActiveTabInSystemSettings();

		FormFields.enableCheckbox(fieldName = "enabled");

		if (IsElementPresent(key_text = "Save", locator1 = "Button#ANY")) {
			PortletEntry.save();
		}
		else {
			PortletEntry.update();
		}

		ValidateRedirectionUpgrade.viewAndAccessRedirects();

		ValidateRedirectionUpgrade.editAndDeleteRedirects();

		ValidateRedirectionUpgrade.ignore404URLs();
	}

}