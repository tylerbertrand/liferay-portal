ext.expectedErrorMessage = "A client extension project must not contain more than one batch or siteInitializer type client extension"
ext.taskPath = ":mixed-batch-and-site-initializer:createClientExtensionConfig"

apply from: project.property("commonScript.shouldFailTestCase.gradle")