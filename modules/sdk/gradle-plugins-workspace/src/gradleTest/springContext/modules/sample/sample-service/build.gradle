buildService {
	apiDir = "../sample-api/src/main/java"
}

dependencies {
	api project(":modules:sample:sample-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

group = "sample"