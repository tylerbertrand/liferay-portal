@default = "isNotEditable"
definition {

	function isNotEditable(locator1) {
		selenium.waitForElementPresent(${locator1}, "true");

		selenium.waitForVisible(${locator1}, "true");

		selenium.isNotEditable(${locator1});
	}

}