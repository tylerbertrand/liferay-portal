@default = "selectWindow"
definition {

	function selectPopUp(value1) {
		selenium.pause(3000);

		selenium.selectPopUp(${value1});
	}

	function selectWindow(value1) {
		selenium.pause(3000);

		selenium.selectWindow(${value1});
	}

	function selectWindowNoSPARefresh(value1) {
		selenium.pause(3000);

		selenium.selectWindow(${value1});
	}

	function selectWindowTopNoSPARefresh() {
		selenium.pause(3000);

		selenium.selectWindow("null");
	}

	function waitForSelectPopUp(value1) {
		selenium.waitForPopUp("", "");

		selenium.selectPopUp(${value1});
	}

}