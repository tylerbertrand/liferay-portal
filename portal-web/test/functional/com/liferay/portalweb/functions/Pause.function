@default = "pause"
definition {

	@summary-ignore = "true"
	function pause(value1) {
		WaitForSPARefresh();

		selenium.pause(${value1});
	}

	@summary-ignore = "true"
	function pauseNoSPARefresh(value1) {
		selenium.pause(${value1});
	}

}