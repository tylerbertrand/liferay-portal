definition {

	@summary = "Default summary"
	macro select(selectFieldValue = null, selectFieldLabel = null) {
		Select(
			key_selectFieldLabel = ${selectFieldLabel},
			locator1 = "Select#ANY",
			value1 = ${selectFieldValue});
	}

}