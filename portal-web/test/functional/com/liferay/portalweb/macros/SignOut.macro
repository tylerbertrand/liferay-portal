definition {

	@summary = "Default summary"
	macro signOut() {
		Navigator.openURL();

		UserBar.signOut();
	}

	@summary = "Default summary"
	macro signOutOfSpecificURL(url = null) {
		Navigator.openSpecificURL(url = ${url});

		UserBar.signOut();
	}

}