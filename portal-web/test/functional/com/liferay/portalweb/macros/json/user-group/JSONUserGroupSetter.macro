definition {

	@summary = "Default summary"
	macro setUserGroupId(userGroupName = null) {
		Variables.assertDefined(parameterList = ${userGroupName});

		var userGroupId = JSONUserGroupAPI._getUserGroupIdByName(userGroupName = ${userGroupName});

		return ${userGroupId};
	}

}